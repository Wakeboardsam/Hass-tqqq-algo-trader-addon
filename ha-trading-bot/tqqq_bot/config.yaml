name: "TQQQ Volatility Bot"
description: "Sequential Allocation Matrix Strategy for IBKR"
version: "1.0.0"
slug: "tqqq_bot"
arch:
  - aarch64
  - amd64
startup: application
options:
  ib_host: "10.0.0.5" # IP of your IB Gateway (often another add-on)
  ib_port: 4001
  reduction_factor: 0.95
  symbol: "TQQQ"
schema:
  ib_host: str
  ib_port: int
  reduction_factor: float
  symbol: str
